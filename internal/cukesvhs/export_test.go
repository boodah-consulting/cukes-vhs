package cukesvhs

// Exported aliases for unexported functions, for use in external test package (cukesvhs_test).

var (
	RenderCommand     = renderCommand
	RenderSteps       = renderSteps
	ResolveConfigPath = resolveConfigPath
)
